version: 2.1
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1-node-browsers
      
    steps:
      - checkout
      
      - run:
          name: Dependencias
          command: Hello world
      
      - run:
          name: Compilar el sitio
          command: date
